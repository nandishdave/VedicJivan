# App
APP_ENV=development
APP_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000

# Database
MONGODB_URI=mongodb://vedicjivan-mongo:27017/vedicjivan

# Auth
JWT_SECRET=  # Generate with: openssl rand -hex 32
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# Stripe
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Email (Resend)
RESEND_API_KEY=
EMAIL_FROM=VedicJivan <noreply@nandishdave.world>
ADMIN_EMAIL=

# Internal endpoints (booking reminders cron)
INTERNAL_SECRET=  # Generate with: openssl rand -hex 32

# Google Calendar (admin only — optional)
GOOGLE_CALENDAR_CREDENTIALS=  # base64-encoded service account JSON key
GOOGLE_CALENDAR_ID=  # e.g. your-email@gmail.com
